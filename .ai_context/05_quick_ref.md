🗒️ Быстрый справочник по режимам и промптам (QA Framework)

🔀 Триггеры режимов
Вставляйте в начало сообщения для переключения контекста ИИ:
| Режим | Для чего |
|---|---|
| `[АРХИТЕКТУРА]` | Схема фреймворка, Component-POM, фикстуры, CI/CD пайплайн, шардирование, обработка тестовых данных |
| `[КОД]` | Генерация тестов/компонентов, рефакторинг, клиенты API/DB, конфиги, `pydantic-settings` |
| `[РЕВЬЮ]` | Поиск антипаттернов (`time.sleep`, глобалы, `assert` в UI), проверка `mypy --strict` / `ruff`, оптимизация локаторов |
| `[СТРАТЕГ]` | План миграции Selenium→PW, выбор TMS, балансировка CI-ресурсов, политика retry/flaky, метрики качества |
| `[ДОКУМЕНТ]` | README, инструкции для QA/Dev, API-контракты, гайды по онбордингу в фреймворк |

🧩 Универсальный шаблон запроса
[РЕЖИМ] {{краткая суть задачи}}

Контекст:
- {{что уже сделано / текущий этап}}
- {{ограничения: GitHub Actions free-tier (2vCPU) / strict typing / Component-POM / без time.sleep / API-first data}}

Требования:
1. {{требование 1 (напр. только expect(), scope=function, валидация конфигов)}}
2. {{требование 2 (напр. docstrings, запуск без правок, изоляция BrowserContext)}}

Ожидаемый результат:
- {{что должно быть на выходе (файлы, команды запуска, формат отчёта)}}
- {{критерий приёмки: например, `pytest -x` проходит, `mypy --strict` без ошибок, артефакты Allure генерируются}}